<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/window_background</item>

    </style>

    <style name="CustomToolBarElements" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/primary_text</item>
    <item name="android:textColorSecondary">@color/secondary_text</item>
    </style>
    
    <style name="imageMarginNormal">
        <item name="android:layout_marginTop">@dimen/marginType16</item>
        <item name="android:layout_marginBottom">@dimen/marginType16</item>
        <item name="android:layout_marginLeft">@dimen/marginType24</item>
        <item name="android:layout_marginRight">@dimen/marginType24</item>
        <item name="android:background">#cccccc</item>
    </style>


</resources>
